// The number that appears the most
#include <bits/stdc++.h>
using namespace std;
const int LENGTH = 10000003;
long long a[LENGTH];
bool checkPrime[LENGTH];
int main()
{
    int number, sum;
    cin >> number >> sum;
    for (int i = 0; i < number; i++)
    {
        cin >> a[i];
    }
    long long dem = 0;
    for (int i = 0; i < number; i++)
    {
        cin >> a[i];
    }
    for (int i = 0; i < number; i++)
    {
        long long sumTemp = a[i];
        while (/* condition */)
        {
            /* code */
        }
        
    }
}